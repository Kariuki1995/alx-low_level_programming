#include "main.h'

/**
 * addition - adds two numbers
 * @c: the first number
 * @d: the second number
 * Return: the sum of both numbers
 */

int add(int c, int d)
{
	int sum = c + d;

	return (sum);
}

